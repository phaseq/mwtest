{
  "verifier": {
    "command": [
      "{{mwVerifierTest}}",
      "--config", "{{input_path}}",
      "--verifier", "{{mwVerifierDll}}",
      "--out-dir", "{{tmp_path}}"
    ]
  },
  "verifierUnit": {
    "command": [
      "{{mwVerifierUnitTest}}",
      "--gtest_filter", "{{input_path}}"
    ],
    "cwd": "{{mwVerifierUnitTestCWD}}"
  },
  "verifierApi": {
    "command": [
      "{{mwVerifierApiTest}}",
      "--gtest_filter={{input_path}}"
    ],
    "cwd": "{{mwVerifierApiTestCWD}}"
  },
  "machsim": {
    "command": [
      "{{machsimtest}}", "newmode",
      "--input-dir", "{{input_path}}",
      "--output-dir", "{{tmp_path}}",
      "--tmp-dir", "{{tmp_path}}",
      "--stand-alone", "1"
    ]
  },
  "cncsim": {
    "command": ["{{mwCNCSimTest}}", "{{input_path}}", "-log", "{{tmp_path}}"],
    "cwd": "{{mwCNCSimTestCWD}}"
  }
}