{
  "verifier": {
    "command": [
      "{{mwVerifierTest}}",
      "--config", "{{input_path}}",
      "--verifier", "{{mwVerifierDll}}",
      "--out-dir", "{{tmp_path}}"
    ]
  },
  "machsim": {
    "command": [
      "{{machsimtest}}", "newmode",
      "--input-dir", "{{input_path}}",
      "--output-dir", "{{tmp_path}}",
      "--tmp-dir", "{{tmp_path}}",
      "--stand-alone", "1"
    ]
  }
}